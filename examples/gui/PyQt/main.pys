# main.pys — Pokemon TCG PyQt6 demo (isolated silo)
# Run: python -m transpiler run examples/gui/PyQt/main.pys

import store
import ui

AppStore appStore = openStore()
PokemonQtApp app = PokemonQtApp(appStore)
app.run()
